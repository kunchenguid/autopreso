name: release-please lockfile sync

# Regenerate package-lock.json on release-please PRs so its root package
# metadata stays synchronized with the version proposed in package.json.

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: write

concurrency:
  group: release-please-lockfile-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  sync:
    if: startsWith(github.head_ref, 'release-please--')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      - run: npm install --package-lock-only --ignore-scripts

      - name: Commit lockfile if it changed
        run: |
          set -eu
          if git diff --quiet package-lock.json; then
            echo "package-lock.json already in sync."
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package-lock.json
          git commit -m 'chore: sync package-lock.json with release-please bump'
          git push
